<%= provide(:title, 'Words') %>
<%= render 'shared/search' %>
<br/>
<h2>Trending Words</h2>
<br/>

<%= will_paginate @words %>

<%= render :partial => 'shared/table',
           :locals => { :entities => @words,
                        :mtype => "word",
                        :sort => 1 } %>

<%= will_paginate @words %>
